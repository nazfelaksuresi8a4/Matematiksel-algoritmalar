import random

def BubbleSort(arr):
    n = len(arr)
    
    for i in range(n):
        for j in range(0,n - i - 1):
            if arr[j] > arr[j+1]:
                arr[j],arr[j+1] = arr[j +1],arr[j]
    
    return arr

array = [random.randint(0,100) for k in range(100)]         
print(str((set(BubbleSort(arr=array)))).replace("{","[").replace("}","]"))    