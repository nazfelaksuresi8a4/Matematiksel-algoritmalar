#medyan ne demektir: Medyan (ya da ortanca) bir anakütle ya da örneklem veri serisini küçükten büyüğe doğru sıraladığımızda, seriyi ortadan ikiye ayıran değere denir.

#pythonda medyan sayı bulma algoritması#

liste = [10,6,4,5,2,37,6,107]

seri = sorted(liste)

seri.remove(max(seri))
seri.remove(min(seri))

n = len(seri)

def medyan_hesapla(array):
    if n == 0:
        return 0
    elif n %2 == 0:
        return (array[n // 2 - 1] + seri[n // 2]) // 2
    else:
        return (array[n // 2])
                 
print(medyan_hesapla(seri))    
