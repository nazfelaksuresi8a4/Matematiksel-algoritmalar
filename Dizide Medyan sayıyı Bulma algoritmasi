#medyan ne demektir: Medyan (ya da ortanca) bir anakütle ya da örneklem veri serisini küçükten büyüğe doğru sıraladığımızda, seriyi ortadan ikiye ayıran değere denir.

#pythonda medyan sayı bulma algoritması#

def medyan_bul(dizi):
    
    sorted_array = sorted(dizi)
    
    sorted_array.remove(max(sorted_array))
    sorted_array.remove(min(sorted_array))
    
    n = len(sorted_array)
    
   
    if n == 0:
            return 0
            
    elif n % 2 == 0:
            return (sorted_array[n // 2 - 1] + dizi[n//2]) / 2
        
    else:
            return (sorted_array[n // 2])

liste = [0,1,4,7,6,8,58,6,9,0]
print(medyan_bul(liste))       
    
    
